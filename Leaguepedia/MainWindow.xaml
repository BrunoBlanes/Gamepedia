<Window x:Class="Gamepedia.Leaguepedia.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:models="using:Gamepedia.Leaguepedia.Models"
		xmlns:local="using:Gamepedia.Leaguepedia"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d">
	<Grid VerticalAlignment="Top">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<ListView Name="LeagueNewsListView"
				  ItemsSource="{x:Bind News, Mode=OneWay}"
				  ScrollViewer.VerticalScrollBarVisibility="Auto"
				  ScrollViewer.VerticalScrollMode="Enabled"
				  Loading="LeagueNewsListView_Loading"
				  HorizontalAlignment="Stretch">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="models:News">
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{x:Bind Region}"
								   Margin="6, 0"
								   Width="30"/>
						<TextBlock Text="{x:Bind Team}"
								   Margin="6, 0"
								   Width="50"/>
						<TextBlock Text="{x:Bind Event}"
								   Margin="6, 0"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</Window>