<Window x:Class="Gamepedia.Lol.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:models="using:Gamepedia.Lol.Api.Models"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d">
	<Grid VerticalAlignment="Top">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!--<ListView Name="LeagueNewsListView"
				  ScrollViewer.VerticalScrollBarVisibility="Auto"
				  ScrollViewer.VerticalScrollMode="Enabled"
				  HorizontalAlignment="Stretch">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="models:News">
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{x:Bind Region}"
								   HorizontalAlignment="Center"
								   VerticalAlignment="Center"
								   Margin="6, 0"
								   Width="24"/>
						<Image Source="{Binding Team.Logo, TargetNullValue={x:Null}}"
							   HorizontalAlignment="Center"
							   Stretch="Uniform"
							   Margin="6, 0"
							   Width="32"/>
						<TextBlock Text="{x:Bind Team.Name}"
								   VerticalAlignment="Center"
								   Margin="6, 0"
								   Width="32"/>
						<TextBlock Text="{x:Bind Event}"
								   VerticalAlignment="Center"
								   Margin="6, 0"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>-->
	</Grid>
</Window>